services:
  mongodb:
    image: mongo:7
    container_name: our-secret-space-mongodb
    restart: unless-stopped
    ports:
      - "27017:27017"
    volumes:
      - mongodb_data:/data/db

volumes:
  mongodb_data: